type HolidayMessageProps = {
  countries: string[];
  locations: string[];
  holidayTypes: string[];
  additionalRequirements: string[];
};

export const holidayMessage = (props: HolidayMessageProps) => {
  const { countries, locations, holidayTypes, additionalRequirements } = props;
  return ``;
};
